import re

if __name__ == "__main__":
    pattern = r"The answer is [:\s]*([-+]?\d*\.?\d+)"
    # match = re.search(pattern, input(), re.IGNORECASE)
    # print(match.group(1) if match else "No match found")
    # # The cost for land would be 2000 * $50 = 100,000\nThe cost for the bricks would be 10000 * 1000 / 1000$ =10000\nThe cost for each roof tile would be 500 * 10$ = 50,000\n\nFinal Calculation:\nTotal construction cost would be 100,000 + 100,000 + 50,000 = $250,000\n\nThe answer is 25000
    # print(type(match.group(1)) if match else "No match found")
    
    print(float(-5.0))